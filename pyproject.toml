[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-session-index"
version = "0.4.5"
description = "Locally index, search, and analyze Claude Code and Codex sessions"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    {name = "Lee Fuhr", email = "hi@leefuhr.com"}
]
keywords = ["claude", "codex", "agent", "session", "search", "index", "sqlite", "fts5"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Documentation",
    "Topic :: Text Processing :: Indexing",
]

[project.scripts]
sessions = "session_index.cli:main"
session-index = "session_index.indexer:main"
session-search = "session_index.search:main"
session-analyze = "session_index.analyzer:main"
session-topic-capture = "session_index.topic_capture:main"

[project.urls]
Homepage = "https://github.com/thehesiod/agent-session-index"
Repository = "https://github.com/thehesiod/agent-session-index"
Issues = "https://github.com/thehesiod/agent-session-index/issues"

[tool.setuptools.packages.find]
include = ["session_index*"]
